# Fixtures

All fixtures are sanitized and contain no real customer data.

- `questionnaires/`: sample XLSX questionnaires used for importer tests (Phase 2).
- `evidence/`: sample evidence files used for evidence FS + export pack tests (Phase 1).
- `golden_export/`: expected deterministic export outputs used in golden tests (Phase 1).
